# daily_digest_agent
This is a small project where I built an agent using OpenClaw that reads messages and creates a simple digest with a Python script.  The idea was to experiment with how an agent can process raw inputs and turn them into something structured and useful. It’s still simple, but it shows the base of an automated workflow.
